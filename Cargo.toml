[package]
name = "mihomo_sdk"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4"
thiserror = "2"
tokio = { version = "1", features = ["process", "sync", "rt", "macros", "time", "net", "io-util"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
httparse = "1"
